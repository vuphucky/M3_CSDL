create database demo;
 use demo;
 create table Student(
 id int,
 name varchar(50),
 age int,
 country varchar(50)
 );
create table Class(
id int,
name varchar(50)
);

create table Teacher(
id int,
name varchar(50),
age int,
country varchar(50)
);
